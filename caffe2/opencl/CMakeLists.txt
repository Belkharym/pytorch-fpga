if(USE_OPENCL)
  set(Caffe2_OPENCL_CPU_SRC
    "${CMAKE_CURRENT_SOURCE_DIR}/context.cc"
  )
  set(Caffe2_OPENCL_CPU_INCLUDE
    "${CMAKE_CURRENT_SOURCE_DIR}"
  )
  set(Caffe2_CPU_SRCS ${Caffe2_CPU_SRCS} ${Caffe2_OPENCL_CPU_SRC} PARENT_SCOPE)
  set(Caffe2_CPU_INCLUDE ${Caffe2_CPU_INCLUDE} ${Caffe2_OPENCL_CPU_INCLUDE} PARENT_SCOPE)
  list(APPEND Caffe2_PUBLIC_OPENCL_DEPENDENCY_LIBS c10_opencl)
endif()
